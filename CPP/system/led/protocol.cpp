#include "protocol.h"
#include <list>
#include <string>

using namespace std;

namespace Protocol
{
  void Protocol::Parse( stringstream const& input )
  {
    // parse version section
    // version V __PROTO_VERSION_SUPPORTED__

    // parse pipe basename section

    // parse commands
    // fill list
  }

  //Protocol& Protocol::getInstance()

}
